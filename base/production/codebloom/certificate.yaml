apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: production-codebloom-tls
  namespace: production
spec:
  secretName: production-codebloom-tls
  dnsNames:
    - production.k8s.codebloom.patinanetwork.org
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
