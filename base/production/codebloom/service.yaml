apiVersion: v1
kind: Service
metadata:
  name: codebloom
  namespace: production
spec:
  selector:
    app: codebloom
  ports:
    - name: http-web
      protocol: TCP
      port: 80
      targetPort: 8080
