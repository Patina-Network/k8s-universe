apiVersion: v1
kind: Service
metadata:
  name: codebloom
  namespace: staging
spec:
  selector:
    app: codebloom
  ports:
    - name: http-web
      protocol: TCP
      port: 80
      targetPort: 8080
