apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: github-membership
  namespace: flux-system
spec:
  interval: 30m
  chart:
    spec:
      chart: ./environments/infrastructure/crossplane/github/members
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 12h
      reconcileStrategy: Revision
