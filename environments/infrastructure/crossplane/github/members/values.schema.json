{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Crossplane GitHub Members Schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "namespace": {
      "type": "string",
      "description": "The namespace where the resources will be deployed."
    },
    "providerConfigName": {
      "type": "string",
      "description": "The Crossplane ProviderConfig name for GitHub."
    },
    "members": {
      "type": "array",
      "description": "List of GitHub organization members and their roles.",
      "items": {
        "type": "object",
        "required": ["username", "role"],
        "properties": {
          "username": {
            "type": "string",
            "description": "The GitHub handle of the user."
          },
          "role": {
            "type": "string",
            "enum": ["admin", "member"],
            "description": "The role assigned within the organization."
          }
        }
      }
    }
  },
  "required": ["namespace", "providerConfigName", "members"]
}
