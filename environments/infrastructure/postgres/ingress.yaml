apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: postgres-tcp
  namespace: infrastructure
spec:
  entryPoints:
    - postgres
  routes:
    - match: HostSNI(`db.k8s.patinanetwork.org`)
      services:
        - name: postgres
          port: 5432
  tls:
    passthrough: true
